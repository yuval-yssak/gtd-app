- [Public-API writable-fields drift](project_writable_fields_drift.md) — `WRITABLE_FIELDS` allowlist is the only gate against caller-clobber of server-managed sync state; Zod validates shape, not authority. Cross-reference fields against `git grep` to find server-managed ones.
- [/v1/ projection `user` field inconsistency](project_v1_projection_user_field_inconsistency.md) — Only `presentItem` still exposes `user`; the other three projections drop it. Phase 3 cleanup pending. Flag drift on new projections.
- [/v1/ router pattern duplication](project_v1_route_pattern_duplication.md) — List-query/cursor parsing copy-pasted across 4 v1 routers. Same for test setup boilerplate. Recommend extraction on new routers.
- [Tenant-isolation test gap on PATCH/DELETE](feedback_tenant_isolation_test_gap.md) — New /v1/ write endpoints consistently ship without tenant-isolation tests; require them per PATCH/DELETE before approving.
- [/v1/ POST handlers skip `OperationValidationError` catch](project_v1_post_handlers_skip_zod_catch.md) — POST often calls `applyAndPublishOperation` without try/catch while PATCH catches it. Fragile if schema/parser drifts. Flag asymmetry on review.
- [/v1/reassign now uses applyAndPublishOperation pipeline](project_reassign_bypasses_apply_pipeline.md) — Both legs through apply pipeline. Person/workContext NOT reassignable; item/routine moves auto-relink refs into recipient via find-or-create.
- [Reassign auto-relink Promise.all mirror-create race](project_reassign_promise_all_mirror_race.md) — relinkPeople/WorkContextIds parallel over find-or-create; same email/name (or duplicate id) → silent N-mirror duplication under toUserId. No unique-index backstop.
- [Reassign mirror orphan on downstream failure](project_reassign_mirror_orphan_on_failure.md) — Mirror persons/contexts created BEFORE preValidate / targetCalendar check / GCal create. Any post-relink failure leaves committed rows + ops + fan-out with no rollback.
- [Reference cascade routine-template blindspot](project_reassign_routine_template_blindspot.md) — Cascades on direct person/workContext delete only scan items, never routines. (Reassign path is now covered by relinkRoutineReferences.)
- [Routine composite must bump updatedTs](project_routine_split_updated_ts_pitfall.md) — splitRoutine/buildCappedHead-style "capped head from DB" snapshots silently lose under LWW if `updatedTs` isn't stamped to `now`. Test persisted state, not response body.
- [replaceById is not user-scoped — _id unguessability is the only tenant guard](project_replaceById_tenant_bypass.md) — `AbstractDAO.replaceById` upserts by `_id` only. A `create` op with a foreign `entityId` clobbers + moves the foreign row to the caller. Pre-existing in /sync/push; Phase 2 widened it to /v1/operations/batch.
- [v1 PATCH broadened-surface drift: trash transitions + dropped `extra`](project_v1_patch_trash_extra_drift.md) — Phase 3 PATCH lets `status:'trash'` through (no /v1 restore exists), and OperationValidationError.failure.extra:{status,field} is silently dropped from 400 responses despite docs/comments promising it.
- [/sync/* fire-and-forget purge tests are timing-trap prone](project_sync_purge_test_timing_traps.md) — Purge-survival tests using `await tick()` + countDocuments can pass spuriously; demand a canary op + `waitForPurge` for positive proof.
- [`$set: doc` with fresh `_id` breaks upsert re-runs silently](project_upsert_set_id_immutable_drift.md) — recurring bug; matched-path fails with ImmutableField code 66 but bulkWrite only logs `writeErrors`. Correct pattern: `$setOnInsert: { _id }` + `$set: <rest>` (see calendarIntegrationsDAO).
- [Calendar restore/relink wiring must gate on cancelled/past/!rrule](project_calendar_restore_ordering_pitfall.md) — Restore-before-guard produces restore-then-trash double ops + brief status flap. Demand tests for the three skip cases on any new relink.
- [lastKnownCalendar* marker becomes orphan on cross-account reconnect](project_lastknown_marker_orphan_risk.md) — Pushback skip is unconditional with no expiry; only a matching inbound event clears it. Cross-account reconnect leaves items silently un-pushable.
- [applyExceptionToItems create-on-miss has no dedupe](project_create_on_miss_no_dedupe.md) — No unique index on `(user, calendarInstanceEventId)` and no in-flight guard. Manual + webhook sync racing for the same config can double-create. Worse under cross-account reconnect where stale instance id survives.
- [Routine-instance `attendees` push is a per-instance override, not an echo](project_routine_instance_attendees_override_pitfall.md) — Policy flipped May 2026: `pushRoutineInstanceOverride` now forwards `snapshot.attendees` unconditionally, gated only by a client-side UI dialog. `buildCalendarItem` mirrors master attendees onto every instance, so any title/notes/time edit now forks that instance silently.
- [`lastPushedToGCalTs` stamped after await can blow echo window](project_lastpushed_ts_stamped_after_await.md) — 5s echo window vs. PATCH+Pub/Sub latency. New pushback paths (RSVP) stamp AFTER await; prefer pre-await stamp or anchor on Google's `data.updated`. Compounds noop-update spam.
- [Routine master attendees propagation now partial](project_routine_master_attendees_no_propagation.md) — `propagateMasterGCalOwnedChangesToItems` + `applyGCalOwnedRoutineDeltaOnly` exist now, but `applyModifiedExceptionToOne`'s bare $unset still corrupts items on exception-only sync cycles. Also: asymmetric race-guard policy in the same file.
- [Id normalization fixes must be symmetric](project_id_normalization_asymmetry_pattern.md) — Normalize-on-write fixes that miss the comparison/read sites silently re-create the same bug from the opposite direction. On any id-transform fix, grep every `.has(`/`=== id`/`{$in:ids}` site for the same field and apply the same transform.
- [Cascade-emitted ops traverse the full pipeline](project_cascade_emitted_ops_traverse_full_pipeline.md) — Reference/breadcrumb cascades that emit ops through applyAndPublishOperations get full GCal/SSE/webhook fan-out; calendar items get title pushed to GCal, routine instances fork. Demand `suppressGCalPushback: true` + regression test.
- [Snapshot-replace defeats LWW on concurrent edits](project_snapshot_replace_defeats_lww_on_concurrent_edits.md) — find→build-snapshot→applyAndPublishOperations(now) pattern silently clobbers concurrent client writes because the LWW guard always passes. Demand conditional updateOne or per-item re-read.
- [AbstractDAO invariance — `as unknown as` double-casts are unsound for write methods](project_abstractdao_invariance_double_cast_unsoundness.md) — `AbstractDAO<T>` is invariant in T. Double-cast helpers (e.g. pickDAOForHydration) compile but break if anyone touches replaceById/insertOne on the cast variable. Prefer narrow structural types.
- [Heal writes that bump updatedTs can clobber concurrent edits](feedback_heal_updated_ts_bump_clobbers_concurrent_edits.md) — Server-internal heal writes (link re-stamps, etc.) that set updatedTs:now risk dropping offline client edits with older updatedTs via the LWW guard. Mirror the `stampItemLastPushed` precedent: don't bump updatedTs for plumbing-only writes.
- [Webhook lock release leaves ghost `running` state when re-run was queued](project_webhook_lock_release_ghost_running.md) — `runWebhookSyncLoop` catch-path calls `finishWebhookSync` but discards its return value; if a delivery arrived during the throwing sync, state ends up `'running'` with no runner and the next delivery is silently coalesced.
- [New unique index needs a boot dedup migration](project_unique_index_needs_boot_dedup_migration.md) — Adding a unique/partial index to an existing collection is a boot-crash risk if violating data exists; require a paired dedup migration run strictly before the index build, keeper tie-break matching the read path, and a violating-fixture test.
- [floatingDateTime shared across item + routine](project_floating_datetime_shared_across_item_and_routine.md) — One schema gates Item.timeStart/timeEnd AND routineExceptions newTimeStart/End; any widen/tighten hits both silently. Demand a test per surface.
- [rsvp ops have snapshot:null but aren't 'delete'](project_rsvp_ops_have_null_snapshot_not_delete.md) — Snapshot-equality/dedup logic that only checks `delete` will collapse distinct rsvp ops (both stringify to 'null'). Special-case opType==='rsvp'.
- [E11000 re-resolve finder must match index predicate](project_e11000_reresolve_status_unfiltered_finder.md) — Race-loser re-resolve via a status-unfiltered finder can return a dead trash twin instead of the live winner; demand a coexisting-trash-row test fixture.
- [Heal/revive to active:true must check sibling-active](project_heal_revive_ignores_sibling_active_unique_index.md) — Reviving a stuck routine to active:true ignores an existing active sibling on the same series → E11000 at runtime + next-boot index crash. Demand a coexisting-active-sibling fixture.
- [routineItemGeneration.test.ts is pre-existingly flaky](project_routine_item_generation_test_flake.md) — ~1/8 runs fail with empty items collection; fire-and-forget fan-out races beforeEach deleteMany. Re-run before blaming the diff.
- [GCal setupWatch relies on stale in-memory config ids](project_gcal_setupwatch_relies_on_stale_inmem_config.md) — Idempotent setupWatch stops the stale channel using the OLD ids carried on the passed in-memory config; any re-read-from-DB or field-clear before setupWatch silently re-opens the orphaned-channel leak.
- [getExceptions window anchored to `since`, not now-30d](project_getexceptions_window_anchored_to_since.md) — Reconcile-on-absence logic that hardcodes a now-30d lower bound over-reconciles past exceptions (real timeMin = max(since, now-30d)); all calendar tests run epoch `since` so the bug is masked.
- [Reconcile window date-vs-datetime truncation](project_reconcile_window_date_vs_datetime_truncation.md) — Mirroring getExceptions' timeMin with a YYYY-MM-DD-truncated windowStart leaves a same-day sliver that silently reverts valid exceptions; compare on the ISO instant.
- [Reconcile window-END sliver asymmetry](project_reconcile_window_end_sliver_asymmetry.md) — Floor sliver fixed with strict `>` but the symmetric now+1y windowEnd left inclusive `<=` (provider timeMax is an instant). Check BOTH boundaries on any reconcile window.
