# better-auth has an optional peerDep on @sveltejs/kit, which pulls in
# @sveltejs/vite-plugin-svelte requiring vite@^8. We don't use SvelteKit,
# so we suppress this phantom peer-dep conflict.
legacy-peer-deps=true
