- [Click-to-edit accessibility](feedback_click_to_edit_a11y.md) — Markdown previews wrapped in role="button" hide content from AT and trap link descendants; flag and propose splitting gesture from content.
- [Page-mode editor autofocus competition](feedback_page_mode_autofocus.md) — Page chrome was changed to suppress title autofocus; watch for *other* fields (notes textarea) silently grabbing focus on mount and re-introducing the same complaint.
- [Inverse-pair predicates](feedback_inverse_pair_predicates.md) — Reviewer-introduced helpers sometimes ship as two near-mirror predicates of one truth; prefer a single canonical predicate and invert at call site.
- [Uncancellable setTimeout in routes](feedback_uncancellable_settimeout_in_routes.md) — Deferred-navigate setTimeouts in route components need re-entry clearTimeout + unmount cleanup; missing either leaves stale navigations firing after the user already moved on.
- [Extracting branches swallows outer-return semantics](feedback_extract_branch_helpers_swallow_outer_return.md) — Moving a fire-and-forget branch into an inner helper demotes its `return` from "exit callback" to "exit helper"; outer postludes (onSaved/onClose/nav) silently fire for that branch.
- [IDB upsert on unique secondary index](feedback_idb_upsert_unique_index_reconcile.md) — delete-stale-then-put in one tx; check tx-liveness, dangling activeAccount pointer, and Promise.all concurrent callers.
- [React keys by display name](feedback_react_keys_by_display_name.md) — list rows keyed by chip label / person / context name (not _id); duplicate names collide since helpers return lossy string[]. Flag it.
- [Client ItemStatus union duplicated](project_client_item_status_union_duplicated.md) — MyDB.ts exports EnergyLevel but NOT ItemStatus; status union is inlined across files. A new local copy is convention, not a fresh violation.
- [Humanize enums via existing label map](feedback_humanize_enum_via_existing_label_map.md) — presenter switches leak raw enums (energy low/medium/high) via String() default; reuse existing label maps + require a test per enum field.
- [Retired-scope negative pin](feedback_retired_scope_negative_pin.md) — removing a union literal: keep a `.not.toContain('X')` pin (don't swap for weaker positive assertion) + render-test the out-of-union value at the network boundary.
- [Passthrough helper un-tests the wiring](feedback_passthrough_helper_untests_wiring.md) — thin forward-the-arg helpers get unit-tested while the load-bearing call-site argument (pivot to item.userId not account.id) stays untested; require a component test that pins the actual arg.
- [Hiding a field by seeding '' silently clears](feedback_hide_field_seed_empty_clears_silently.md) — hiding an input + seeding its state '' makes in-place edit emit a clear-patch for legacy data; self-consistent but untested (existing tests seed empty). Require the clear + omit regression tests.
- [New side effect skips existing mutation test](feedback_new_side_effect_skips_existing_mutation_test.md) — side effect threaded into clarify*/removeItem gets a new-lib test but itemMutations.test.ts is left without the wiring assertion (incl. guard negative cases). Require it there too.
- [Ghost/optimistic rows leak into derived counts](feedback_ghost_rows_leak_into_derived_counts.md) — merging ephemeral rows upstream of a page's status filter inflates every count/empty-state/wizard-input derived from it; audit each consumer for live-only vs all-rows.
- [Autosave content-sync side effect untested](feedback_autosave_content_sync_side_effect_untested.md) — propagation call threaded into an autosave commit (persistRoutineText) is tested only as a pure helper; require a burst-chaining test pinning `previous` = prior persisted state.
- [SW postMessage branch untested + any receiver](feedback_sw_postmessage_branch_untested.md) — new SW→tab message types ship without the SW-side test AND the tab receiver reads event.data.<field> as any; require both a post test and a typeof narrow.
- [Shared search hook writeUrlQuery stability](feedback_shared_search_hook_writeurlquery_stability.md) — useListSearch takes injected writeUrlQuery; call sites spreading full urlState churn its identity + re-fire the debounce effect. Verify early-return guard + merge-latest.
- [Virtualization breaks exact DOM counts](feedback_virtualization_breaks_exact_dom_counts.md) — WindowVirtualizer voids every exact row-count assertion (unit+e2e) and needs an in-page search to replace find-in-page.
- [Calendar action hooks untested wiring](feedback_calendar_action_hooks_untested_wiring.md) — useSyncNow/useRepairSync clones test API+formatter in isolation but never the API→syncAndRefresh→setSummary composition; ask for a hook test.
- [Show button calls toggle](feedback_show_button_calls_toggle.md) — one-way "Show/Enable X" action wired straight to a symmetric toggle*() can re-hide when state changed underneath (snackbar left open); require a direction guard + no-op-when-already-shown test.
